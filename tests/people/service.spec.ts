import { describe, test, expect, beforeAll } from "@jest/globals";

describe('Validate people services - unit tests', () => {
    test('Should create a people with cpf document', async () => {
        
    });
})